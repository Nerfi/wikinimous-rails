<h1> Here you got your articles</h1>
<ul>
<%= @articles.each do |article| %>
<li><strong><%= article.title %></li>
<ul><%= article.content %></ul>
</ul>
<%= link_to "add a new article", new_path %>
<br>
<%= link_to 'Delete', article_path(@articles),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end%>

